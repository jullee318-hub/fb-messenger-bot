# 品慧老師 FB Messenger 機器人 — 設定教學

## 全部只要 4 個步驟！

---

## 步驟一：申請 Facebook 開發者帳號 & 建立應用程式

1. 用你的 Facebook 帳號登入 https://developers.facebook.com/
2. 點右上角「我的應用程式」→「建立應用程式」
3. 選擇「商業」類型
4. 填入應用程式名稱（例如：品慧老師小助手）
5. 建立後，在左側選單找到「Messenger」→「設定」
6. 在「存取權杖」區塊，選擇你的粉絲專頁，點「產生權杖」
7. **複製這組權杖**（這就是 `FB_PAGE_ACCESS_TOKEN`）
8. 在「設定」→「基本資料」中找到「應用程式密鑰」
9. **複製密鑰**（這就是 `FB_APP_SECRET`）

---

## 步驟二：取得 Claude AI 金鑰

1. 前往 https://console.anthropic.com/
2. 註冊帳號（用 Google 帳號即可）
3. 進入後，點左側「API Keys」
4. 點「Create Key」建立金鑰
5. **複製金鑰**（這就是 `ANTHROPIC_API_KEY`）

> 注意：Claude API 是按使用量收費的，一般客服用量每月大約 NT$100-300 左右

---

## 步驟三：部署機器人到雲端（以 Railway 為例）

### 方法 A：用 Railway（最簡單，推薦）

1. 前往 https://railway.app/ 並用 GitHub 帳號登入
2. 點「New Project」→「Deploy from GitHub repo」
3. 選擇你上傳了這個專案的 GitHub 倉庫
4. 部署後，點「Variables」加入以下環境變數：

```
FB_PAGE_ACCESS_TOKEN = （步驟一複製的權杖）
FB_VERIFY_TOKEN = pinhuibot2024（自訂密碼，等等要用）
FB_APP_SECRET = （步驟一複製的密鑰）
ANTHROPIC_API_KEY = （步驟二複製的金鑰）
LINE_URL = https://line.me/R/ti/p/你的LINE_ID
```

5. 部署完成後，Railway 會給你一個網址，例如：
   `https://你的專案名.up.railway.app`

### 方法 B：用 Render（免費方案可用）

1. 前往 https://render.com/ 並註冊
2. 「New」→「Web Service」→ 連結 GitHub 倉庫
3. Runtime 選 Node，Start Command 填 `npm start`
4. 在 Environment 加入跟上面一樣的環境變數
5. 部署完成後會得到一個網址

---

## 步驟四：設定 Facebook Webhook

1. 回到 Facebook 開發者後台 https://developers.facebook.com/
2. 進入你的應用程式 →「Messenger」→「設定」
3. 在「Webhooks」區塊，點「新增回呼網址」
4. 填入：
   - **回呼網址**：`https://你的railway網址/webhook`
   - **驗證權杖**：`pinhuibot2024`（跟環境變數裡的 FB_VERIFY_TOKEN 一樣）
5. 點「驗證並儲存」
6. 在「Webhook 欄位」勾選 `messages` 和 `messaging_postbacks`
7. 在「訂閱」區塊選擇你的粉絲專頁

---

## 完成！🎉

現在去你的粉專 Messenger 發一條訊息測試看看！

---

## 如何修改關鍵字回覆？

打開 `src/config/keywords.json` 這個檔案，格式如下：

```json
{
  "triggers": ["價格", "多少錢"],     ← 觸發關鍵字（收到這些字就回覆）
  "reply": "這是回覆內容",            ← 回覆的文字
  "addLineLink": true                 ← 是否附上 LINE 連結（true/false）
}
```

要新增關鍵字，在 `keywords` 陣列裡多加一組就好。

---

## 如何修改 AI 助手的個性？

打開 `src/config/botPersona.js`，修改 `SYSTEM_PROMPT` 裡面的文字即可。

---

## 常見問題

**Q: 機器人沒有回覆？**
A: 檢查 Railway/Render 的 Log，看有沒有錯誤訊息

**Q: 回覆太慢？**
A: AI 回覆通常需要 2-3 秒，這是正常的

**Q: 想暫停機器人？**
A: 在 Railway/Render 暫停服務即可

**Q: LINE 連結按鈕沒出現？**
A: 確認 .env 裡的 LINE_URL 有填寫正確
